class Recipe {
  String title;
  String user;
  String description;
  bool isFavorite;
  int favoriteCount;

  Recipe(this.title,this.user,this.description,
      this.isFavorite, this.favoriteCount);
}